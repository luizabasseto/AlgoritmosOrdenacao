class Buscas:
    def __init__(self, lista, n):
        self.__lista = lista
        self.__n = n
        
    def buscaBinaria(self,elemento):
        lista = self.__lista
        for i in range(self.__n):
            
        
        
        
        return elem
    
    def buscaLinear(self,elemento):
        lista = self.__lista
        for i in range(self.__n):
            if(lista[i]==elemento):
                return i
            
        return -1   